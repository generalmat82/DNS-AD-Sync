The .env file contains the following parameters:

`dns_hostname` -> record to update.
`realm` -> AD domain
`zone` -> DNS zone to apply to
`server` -> DNS server
`keytab_file` -> location of the keytab file in relation to the location of the script.
`username` -> name of the AD user
`TTL` -> Time to live of the record.
